spring:
  application:
    name: jev-document-classifier
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 100MB

server:
  port: 8080

jev:
  api-key: ${OPENROUTER_API_KEY:}
  base-url: ${OPENROUTER_BASE_URL:https://openrouter.ai}
  model: ${JEV_MODEL:typesafe/jev-1.13}

ocr:
  tesseract-command: ${OCR_TESSERACT_COMMAND:tesseract}
  languages: ${OCR_LANGUAGES:eng+deu}

classification:
  same-document-threshold: ${SAME_DOCUMENT_THRESHOLD:0.75}
